# Common errors

## gtp5g kernel module missing

If integration tests fail early, ensure the `gtp5g` kernel module is installed and loaded.

<!---
TODO: Add a non-Debian installation path if needed.
The script uses `apt-get`.
-->
